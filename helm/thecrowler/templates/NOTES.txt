The CROWler Helm release is installed.

Release:
  {{ .Release.Name }}

Namespace:
  {{ .Release.Namespace }}

Runtime configuration:
  ConfigMap: {{ include "thecrowler.configMapName" . }}

Credentials:
  Secret: {{ include "thecrowler.secretName" . }}

{{- if .Values.api.enabled }}

Access the API locally:
  kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "thecrowler.apiName" . }} 8080:{{ .Values.api.service.port }}
{{- end }}

{{- if .Values.jaeger.enabled }}

Access Jaeger locally:
  kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "thecrowler.jaegerName" . }} 16686:16686
{{- end }}

Inspect workloads:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get services -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

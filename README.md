# The CROWler Deployment Support

This repository contains the necessary files and scripts to support the deployment of the CROWler framework in both development and production environments.

It's organized by deployment tool, and there are a couple of directories (`scripts` and `common`) that are useful on all deployment types.
